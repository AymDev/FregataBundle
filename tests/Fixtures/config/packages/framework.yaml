framework:
    test: true

    router:
        utf8: true
        resource: kernel::loadRoutes
        type: service

    messenger:
        reset_on_message: true

    session:
        storage_factory_id: session.storage.factory.mock_file